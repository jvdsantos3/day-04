# STATE

## Decisions

### AD-001
- **Decision**: Stack web FastAPI + Jinja2 + HTMX para MVP
- **Reason**: Stack Python unificada; auth + dashboard + SSE sem SPA complexa
- **Trade-off**: UX menos rica que React; suficiente para MVP
- **Scope**: Todas as features web do financial-assistant
- **Date**: 2026-07-02
- **Status**: active

### AD-002
- **Decision**: Autenticação JWT em cookie httpOnly + bcrypt; dados isolados por user_id
- **Reason**: Auth simples solicitada (nome, email, senha); stateless; multi-usuário
- **Trade-off**: Sem refresh token rotation no MVP
- **Scope**: SQLite, ChromaDB, MCPs, agentes — todo acesso filtra user_id
- **Date**: 2026-07-02
- **Status**: active

### AD-003
- **Decision**: Embeddings locais via intfloat/multilingual-e5-small (384 dims)
- **Reason**: DeepSeek sem API de embeddings; zero custo; PT-BR
- **Trade-off**: Download inicial do modelo; CPU-only no dev
- **Scope**: ChromaDB collections, chroma-mcp, indexer
- **Date**: 2026-07-02
- **Status**: active

### AD-004
- **Decision**: MCPs finance-mcp e chroma-mcp como sub-processos com fallback in-process
- **Reason**: Extensibilidade e isolamento de domínio solicitados
- **Trade-off**: Latência no cold start
- **Scope**: LangGraph agents, tool loading
- **Date**: 2026-07-02
- **Status**: active

## Handoff

- **Feature**: financial-assistant / `.specs/features/financial-assistant/`
- **Phase / Task**: Phase 3 (Domain Services) COMPLETA — T9 (`344a5f1`), T10 (`f7b1878`), T11 (contracts, `49e9925`/fix `1b06036`) done. Phase 4: T12 (`0661b22`), T13 (`8572145`), T14 (indexer, `8b1c324`) e T15 (knowledge seed) done. Phase 5 COMPLETA — T16 (`finance-mcp`, `633c214`) done, T17 (`chroma-mcp`) done, T18 (MCP client adapter + fallback) done. Phase 6 (Agents) COMPLETA — T19 (LangGraph skeleton) done, T20 (orchestrator intent routing, `f1f76c3`) done, T21 (Atendimento specialist, `62b951a`) done, T22 (Transações specialist, `2644173`) done, T23 (Orçamento specialist, `53146fe`) done, T24 (Validator node) done, T25 (wire full graph) done. Phase 7 (Web UI) COMPLETA — T26 (base templates) done, T27 (dashboard page) done, T28 (dashboard HTMX filters) done, T29 (chat page + SSE) done.
- **Completed**: Specify, Design, Tasks, T1–T29
- **In-progress**: none
- **Next step**: Phase 8 — T30/T31/T32 (cenários conversacionais reais em `tests/integration/test_conversation_scenarios.py`, os 3 prompts literais do spec.md). Depois disso, a Verificação final automática do Verifier roda (última task da feature).
- **Blockers**: none — o erro de coleção do T9 (`list[...]` sombreado pelo método `list`) foi resolvido com `from __future__ import annotations` no repositório; suíte completa (unit + integration) passa (187 testes).
- **Uncommitted files**: none
- **T3 note**: `BudgetCategory`/`TransactionType` enums vivem em `domain/models.py`; T4 reutiliza `BudgetCategory` (não recriar). `Transaction.category` é nullable no DB; invariante tipo↔categoria fica no contrato Pydantic `TransactionCreate` (T11), conforme design. Alembic: `script_location` em `alembic.ini`; `env.py` lê `database_url` de `get_settings()` e importa `domain.models` para autogenerate. Rodar migrations com `.venv/bin/alembic upgrade head` (precisa `data/` existir — gitignored).
- **Spec-precision gap (T2)**: `JWT_EXPIRE_MINUTES=1440` e `DEEPSEEK_BASE_URL=https://api.deepseek.com` sem valor definido na spec — defaults escolhidos na implementação
- **T5 note (SPEC_DEVIATION)**: design named `passlib[bcrypt]`, mas passlib 1.7.4 não inicializa o backend com bcrypt 5.0.0 instalado. `auth/service.py` usa a lib `bcrypt` direto (`hash_password`/`verify_password`); `pyproject.toml` troca `passlib[bcrypt]` → `bcrypt`. T6/T7 devem reusar essas funções — não reintroduzir passlib. Gap de precisão: spec silenciosa sobre senhas >72 bytes → sem tratamento (bcrypt levanta ValueError).
- **Spec-precision gap (T4)**: Prazeres é `≥ 5%` (sem teto na spec) mas `BudgetTarget.max_pct` é float não-nulável → teto modelado como `100.0` em `domain/budget_defaults.py`. BudgetService (T10) deve tratar Prazeres como sem-alerta-por-excesso.
- **T6 note**: App factory `create_app()` em `main.py` (inclui `auth.router`); T7/T27/T29 devem registrar seus routers ali. DB dependency `get_db()` em `db/session.py` (testes fazem `app.dependency_overrides[get_db]` com SQLite `:memory:` + StaticPool — ver `tests/integration/test_auth.py` fixture `client`). `python-multipart` adicionado ao `pyproject` (parsing de `Form`). Register redireciona 303 → `/dashboard` **sem** setar cookie de sessão — auto-login/JWT é escopo do T7. Templates em `web/templates/`; `register.html` é standalone (base layout vem no T26 — pode refatorar para `extends base.html` depois). Gaps de precisão: status de erro de validação (senha curta / email duplicado) não definido na spec → escolhido `400` com re-render do form.
- **T7 note**: JWT helpers em `auth/service.py`: `create_access_token(subject)` / `decode_access_token(token) -> str|None` (HS256, `sub`=user.id, `exp` de `jwt_expire_minutes`). Cookie de sessão `access_token` (const `SESSION_COOKIE_NAME` em `auth/dependencies.py`), `httponly=True, samesite="lax"`, `max_age=jwt_expire_minutes*60`. `get_current_user` (dependency) lê o cookie, decodifica, carrega `User` por UUID e retorna; se ausente/inválido/expirado → `HTTPException(302, Location=/login?next=<path>)`. **Boundary shift**: AUTH-05 (redirect) foi implementado no T7 (não no T8) porque o teste `test_protected_route_redirect` estava no verify do T7. Stub protegido `GET /dashboard` em `web/router.py` (retorna `<h1>Olá, {name}</h1>`) — **T27 substitui** pelo dashboard real (tabela + barras). `web_router` já incluído em `main.create_app`. Login inválido devolve msg única `"Email ou senha inválidos"` (400) p/ senha errada E email inexistente (AUTH-04 — não vazar existência). Gaps de precisão: status não definidos na spec → 303 (login/logout success, POST→GET), 302 (guard redirect), 400 (falhas de form/login).
- **T8 note**: `auth/dependencies.py` agora tem **dois** dependencies compartilhando `_resolve_user(request, db) -> User|None`: `get_current_user` (rotas web → 302 redirect `/login?next=`) e **`get_current_user_api`** (rotas API → `HTTPException(401)`). T29 (`POST /api/chat`) deve usar `get_current_user_api`. Design listava só `get_current_user(token)` — o split web/API é elaboração exigida pela ação 3 do T8 (401 p/ API), não é SPEC_DEVIATION. **Ordering AUTH-06**: `test_user_isolation_in_repository` foi coberto no nível do dependency (não do `TransactionRepository`, que é T9 e ainda não existe): fixture `api_client` em `tests/integration/test_auth.py` monta uma rota-probe `GET /api/_probe/my-transactions` guardada por `get_current_user_api` que filtra `Transaction.user_id == user.id` — prova que a sessão autenticada só enxerga os próprios dados. T9 deve replicar esse isolamento dentro do `TransactionRepository` (todas as queries filtram `user_id`).
- **T12 note**: `vector/client.py` expõe `COLLECTIONS` (5 nomes), `get_chroma_client(path=None)` (PersistentClient, default = `settings.chroma_path`), `get_or_create_collections(client) -> dict[name, Collection]` (idempotente) e `require_user_id(metadata) -> dict` (guard AD-002: levanta `ValueError` se `user_id` ausente/vazio/None; retorna cópia). Módulo é **embedding-agnostic** de propósito — T13 (modelo) e T14 (indexer) geram os embeddings; o client só cria o store + coleções. T14/T17 devem reusar `require_user_id` antes de todo `add/upsert`. Testes usam `tmp_path` (não tocam `./data/chroma`) e passam embeddings explícitos p/ não baixar o modelo default do ChromaDB. Gate rodado com `.venv/bin/python -m pytest` (rtk/pytest global usa env sem o pacote instalado → "No tests collected").
- **T13 note**: `vector/embeddings.py` expõe `get_embeddings() -> HuggingFaceEmbeddings` (singleton via `lru_cache`) e as constantes `EMBEDDING_MODEL_NAME` (`intfloat/multilingual-e5-small`) e `EMBEDDING_DIMENSION` (`384`). `device="cpu"`, `normalize_embeddings=True` (cosine-ready para ChromaDB). Dependência nova `sentence-transformers` adicionada ao `pyproject.toml` (requerida pelo backend do `langchain-huggingface`; download do modelo do HF Hub confirmado funcional no ambiente — ~10s no teste). T14 deve consumir `get_embeddings()` para gerar os vetores antes do `upsert` nas coleções do T12.
- **T14 note**: `vector/indexer.py` expõe `index_transaction(user_id, transaction) -> None` e `delete_transaction_embedding(user_id, transaction_id) -> None`, ambos sem parâmetro de coleção — resolvem o client/coleção internamente via `vector.client.get_chroma_client()` + `get_or_create_collections()` (mesmo padrão de teste do T12: monkeypatch em `vector.client.get_settings` para redirecionar a um `tmp_path`). Metadata gravada: `user_id`, `transaction_id`, `category` (valor do enum ou **ausente** — chromadb 1.5.9 descarta chaves de metadata com valor `None` na escrita, então receitas simplesmente não têm a chave `category`), `amount` (float), `date` (ISO). `delete_transaction_embedding` usa `where={"user_id": ...}` combinado a `ids=[...]` — um `user_id` incorreto não apaga o vetor de outro usuário (AD-002). Qualquer falha na tentativa de indexar (embedding **ou** o próprio upsert no ChromaDB) é capturada, logada (`logger.exception`) e enfileirada em `get_pending_reindex()` (fila em memória, sem persistência — `clear_pending_reindex()` para um futuro worker consumir); a chamada nunca propaga exceção, pois o SQLite já foi gravado pelo caller antes de `index_transaction` rodar. **Pendência real**: T14 só implementa as funções — nenhum caller ainda as invoca; isso deve acontecer em T16 (`finance-mcp.create_transaction`/`update_transaction`/`delete_transaction`), por isso VEC-01/VEC-04 continuam **Partial** na tabela de rastreabilidade.
- **T10 note**: `domain/services/budget_service.py` expõe `BudgetService(session).get_summary(user_id, month="YYYY-MM")` retornando dataclasses `BudgetSummary` (`month`, `total_income: Decimal`, `has_income: bool`, `warning: str|None`, `categories: list[CategoryBudget]`) e `CategoryBudget` (`category`, `spent`, `pct: float`, `min_pct/max_pct/target_pct`, `status: "ok"|"alerta"`, `remaining_pct` = `max_pct-pct`, `over_amount: Decimal`). Regras: `%` sobre receita total do mês; `alerta` **só** quando `pct > max_pct` (BUD-02, `over_amount = spent - max%·receita`); abaixo do mínimo **não** alerta; Prazeres (`max_pct=100`) nunca alerta por excesso; receita 0 → `has_income=False` + `warning="sem receita base"` (const `NO_INCOME_WARNING`), sem percentuais. Reusa `TransactionRepository.list` (T9) p/ escopo user_id + mês. **T11** deve criar o contrato Pydantic `BudgetSummary` (design) — hoje é dataclass; **T21/finance-mcp** e **T22/agente Orçamento** consomem `get_summary` (ferramenta `get_budget_summary`); **T23/dashboard** usa os cards de `categories`. BUD-01/02/03 e CONV-03 ficam **Partial** até o agente (T22) e dashboard (T23).
- **T15 note**: `vector/knowledge_seed.py` expõe `seed_knowledge_base()`, `seed_category_examples()`, `seed_all()` e `query_knowledge(query, n_results=3)`. Seeds são **globais** (não pertencem a um usuário): `knowledge_base`/`category_examples` guardam conteúdo compartilhado (regras de categoria, exemplos rotulados), mas `require_user_id` (AD-002, T12) exige `user_id` não-vazio em toda escrita — resolvido com sentinela `GLOBAL_USER_ID = "system"` nos metadados desses dois seeds (spec-precision gap: AD-002 fala em "todo acesso filtra user_id", mas não cobre conteúdo não pertencente a um único usuário). `seed_knowledge_base` grava 5 docs de regra (um por `BudgetCategory`, com faixa % + exemplos da spec) + 1 doc `kb-overview` sem `category` (resumo das 5 categorias, sem teto de "Prazeres" cf. T4). `seed_category_examples` grava 12 descrições rotuladas (`CATEGORY_EXAMPLE_SEEDS`), incl. `"compra no mercado" → custos_fixos` e `"cinema com amigos" → prazeres`. Ambos usam `upsert` com ids fixos (`kb-<categoria>`, `ex-<categoria>-N`) — idempotente, seguro rodar em todo startup. `query_knowledge` reusa `_get_collection`/`get_embeddings` (mesmo padrão do T14) e retorna `[]` se a coleção ainda não foi semeada (sem lançar). **T17** (`chroma-mcp.query_knowledge`) deve envolver esta função; **T21** (Atendimento) é o consumidor final. `find_similar_transactions` (linha do T15 na spec) foi propositalmente **não** implementado aqui — pertence ao `chroma-mcp` (T17), que combina `transactions` + `category_examples`.
- **T11 note**: `contracts/transaction.py` expõe `TransactionCreate` (Pydantic v2, `model_validator(mode="after")`) — `category` obrigatório se `type == EXPENSE`, proibido se `type == INCOME` (VAL-01/VAL-02); `amount` exige `> 0`. `contracts/budget.py` espelha as dataclasses do T10 como `BudgetSummary`/`CategoryStatus` (`ConfigDict(from_attributes=True)`, constroem direto via `model_validate(dataclass_instance)` sem reimplementar a lógica de orçamento). `contracts/agent_response.py` segue **literalmente** o bloco "AgentResponse (contract)" de `design.md` — `AgentResponse(text, suggested_category, action: Literal["none","offer_register","registered"], metadata: dict)` — e a tabela de roteamento de intenção (Onda 15/T20): `Intent` enum com `explain_budget`/`categorize`/`budget_advice`/`register_transaction` (não os nomes genéricos `duvida_geral`/`consultar_orcamento` cogitados inicialmente — corrigido no commit de fix `1b06036` após conferir `design.md`). `IntentClassification(intent, confidence: float 0–1)` é o structured-output do Orquestrador (T20). **T19/T20** devem reusar este `Intent` enum (não recriar); **T22/23/21** (specialists) retornam `AgentResponse`; o Validador (T20+ node) usa `AgentResponse` para o gate de qualidade (design.md "Validator"). CHAT-02/VAL-01/VAL-02 ficam **Partial** — o contrato existe, mas nada ainda o consome (T19+ liga ao grafo).
- **T16 note**: `mcp_servers/finance/server.py` expõe um `FastMCP("finance-mcp")` com 6 `@mcp.tool()`: `create_transaction`, `list_transactions`, `get_budget_summary`, `get_balance`, `update_transaction`, `delete_transaction` — todos com `user_id: str` posicional obrigatório (sem default), convertido p/ `uuid.UUID` internamente. `@mcp.tool()` devolve a função original inalterada (confirmado na lib `mcp` 1.28.1) — os testes chamam as tools diretamente como funções Python (sem subir subprocesso stdio), no mesmo espírito de "MCPs testados isoladamente" do design. Cada tool abre sua própria sessão via `SessionLocal()` (módulo importa o nome de `db.session` — testes isolam sobrescrevendo `finance_mcp.SessionLocal`, mesmo padrão de monkeypatch usado em `indexer.get_embeddings` no T14). **Write-through fechado**: `create_transaction` e `update_transaction` chamam `indexer.index_transaction` após o commit; `delete_transaction` chama `indexer.delete_transaction_embedding` — a pendência apontada na nota do T14 está resolvida; VEC-01/VEC-04 saem de **Partial**. `mcp_servers/finance/__main__.py` foi adicionado (não estava nas Actions literais do T16, mas é o entrypoint que o `MultiServerMCPClient` do design.md invoca via `python -m mcp_servers.finance` — sem ele o comando não roda) — chama `mcp.run()` (stdio default). **Spec-precision gaps**: (1) `get_balance(user_id, month=None)` — spec só diz "Saldo e totais do período" sem fórmula exata; implementado como `total_income - total_expense` do `TransactionRepository.list` filtrado pelo `month` opcional (all-time se omitido); (2) `update_transaction` trata `None` em qualquer campo como "não alterar" (não há como sinalizar "limpar categoria" via MCP hoje — não surgiu caso de uso no MVP). **T17** (chroma-mcp) e **T18** (MCP client adapter) são os próximos consumidores; nenhum agente ainda invoca `finance-mcp` (isso é T19+).
- **T17 note**: `mcp_servers/chroma/server.py` expõe `FastMCP("chroma-mcp")` com 5 `@mcp.tool()`: `search_transactions`, `find_similar_transactions`, `query_knowledge`, `get_chat_context`, `save_working_memory` — mesmo padrão do T16 (`user_id: str` obrigatório sem default, `@mcp.tool()` devolve a função original, tools testadas como chamadas diretas, `__main__.py` com `mcp.run()`). Toda query nas coleções por-usuário (`transactions`, `chat_memory`, `working_memory`) filtra `where={"user_id": ...}`; `query_knowledge` e a metade `category_examples` de `find_similar_transactions` continuam **globais** (`GLOBAL_USER_ID`, T15) — `user_id` é aceito só por consistência de assinatura, não filtra linhas nelas. **Score**: `_similarity(distance) = 1 - distance/2` — ChromaDB usa L2 ao quadrado por padrão (confirmado empiricamente: vetores ortogonais unitários → distância 2.0), e com embeddings normalizados (T13) isso equivale exatamente a `1 - cos`. **Spec-precision gap (VEC-02)**: "score ≥ limiar configurável" não definia o valor — adicionado `Settings.chroma_similarity_threshold: float = 0.5` (só `search_transactions` filtra por ele; `find_similar_transactions`/`get_chat_context` devolvem os `n_results` mais próximos sem corte, pois a spec não pede limiar ali). **VEC-05 fallback**: só `search_transactions` tem fallback (para `TransactionRepository.search_by_description`, capturando `except Exception` ao redor da chamada ChromaDB inteira — client/coleção/query) — nenhum outro tool tem fallback SQL (design.md só menciona o fallback para busca de transações). `find_similar_transactions` combina `transactions` (user-scoped) + `category_examples` (global) ordenados por score desc, resolvendo a pendência apontada na nota do T15. **T18** (MCP client adapter) é o próximo consumidor; nenhum agente ainda invoca `chroma-mcp` (isso é T19+/T21+).
- **T18 note**: `mcp/client.py` expõe `MCP_CONNECTIONS` (dict `{"finance": StdioConnection, "chroma": StdioConnection}`, cada uma `{"transport": "stdio", "command": "python", "args": ["-m", "mcp_servers.<nome>"]}` — literal do snippet conceitual de design.md), `in_process_tools() -> list[BaseTool]` e `async def get_mcp_tools(client=None) -> list[BaseTool]`. `get_mcp_tools` instancia `MultiServerMCPClient(MCP_CONNECTIONS)` (ou usa o `client` injetado — só para teste, sem uso em produção ainda) e tenta `await client.get_tools()`; qualquer exceção (spawn do subprocesso, handshake MCP) é capturada, loga `logger.warning(..., exc_info=True)` e retorna `in_process_tools()` (MCP-03). **Fallback in-process não reimplementa lógica**: `in_process_tools()` envolve as 11 funções Python já existentes de `mcp_servers.finance.server` (6) + `mcp_servers.chroma.server` (5) via `StructuredTool.from_function` — `@mcp.tool()` devolve a função original inalterada (nota do T16), então chamar essas funções direto (sem subprocesso stdio) é literalmente a "tool in-process equivalente" pedida pela spec. Teste `test_mcp_fallback_on_failure` injeta um cliente fake cujo `get_tools()` levanta `RuntimeError` e verifica (1) o conjunto de nomes das 11 tools de fallback e (2) que um warning com "falling back to in-process tools" foi logado (`caplog`) — não sobe subprocesso real, mesmo espírito de "MCPs testados isoladamente" do T16/T17. **Pendência real**: nenhum agente ainda chama `get_mcp_tools()` — isso é T19+ (LangGraph skeleton) quando os specialists carregarem as tools no grafo.
- **T19 note**: `agents/state.py` porta literalmente o `AgentState` TypedDict de design.md (`messages: Annotated[list[BaseMessage], add_messages]`, `user_id`, `session_id`, `intent`, `retrieved_context`, `pending_action`, `agent_notes`, `last_tool_results`, `validation_attempts`, `final_response: AgentResponse | None` — reusa o `AgentResponse` do T11, não recria). `agents/graph.py` expõe `build_graph() -> CompiledStateGraph`: só os 2 nós estruturais pedidos pela Action do T19 (`orchestrator_node`, `validator_node`, ambos stubs que retornam `{}`) — `START -> orchestrator -> validator -> END`, sem branch de especialistas (T21-23) nem aresta de retry `validator -> orchestrator` (design.md mermaid `rejected (retry ≤2)`), porque nenhum especialista existe ainda; isso é resolvido no T25 ("Wire full graph"). **Sem teste pytest dedicado** — a Action/Verify do T19 pede só "graph compila sem erro" (comando `python -c` smoke), não um arquivo de teste; T20 (`test_orchestrator_routing.py`) e T25 (`test_graph_smoke.py`) são os primeiros testes automatizados do grafo.
- **T20 note**: `agents/orchestrator.py` expõe `classify_intent(message, llm=None) -> IntentClassification` (structured output via `ChatOpenAI.with_structured_output(IntentClassification)`; `llm` injetável para teste — default é `get_orchestrator_llm()`, um `ChatOpenAI(model="deepseek-chat", ...)` lazily built com `Settings.deepseek_api_key`/`deepseek_base_url`, cacheado via `lru_cache`), `specialist_for_intent(intent, confidence=1.0) -> str` (mapa `SPECIALIST_BY_INTENT` literal de design.md — `explain_budget`→atendimento, `categorize`/`register_transaction`→transacoes, `budget_advice`→orcamento) e `orchestrator_node(state) -> dict` (lê `state["messages"][-1].content`, chama `classify_intent`, retorna `{"intent": classification.intent.value}`). `graph.py`'s `orchestrator_node` stub foi substituído por este (import, não redefinição). **ORCH-02 (intenção ambígua → Atendimento)**: `specialist_for_intent` recebe `confidence` e retorna `"atendimento"` abaixo de `AMBIGUITY_CONFIDENCE_THRESHOLD = 0.5` (spec-precision gap — a spec não define o limiar; escolhido 0.5) — isso não está amarrado ao grafo ainda (não há campo de "specialist" no `AgentState`), é usado pelo conditional-edge que **T25** vai adicionar. `tests/unit/test_orchestrator_routing.py` mocka o LLM com um fake `with_structured_output(schema).invoke(...)` (sem chamar DeepSeek) cobrindo os 4 padrões da tabela + o fallback de ambiguidade + `orchestrator_node` via `monkeypatch` do `classify_intent` do módulo. **T21/22/23** (specialists) devem reusar `SPECIALIST_BY_INTENT`/`specialist_for_intent` em vez de recriar o mapa.
- **T21 note**: `agents/specialists/atendimento.py` expõe `query_knowledge` (LangChain `@tool` que envolve `vector.knowledge_seed.query_knowledge`, `n_results=6` — a `knowledge_base` só tem 6 docs hoje: 5 categorias + `kb-overview`, T15; buscar todos garante que faixas **e** exemplos apareçam mesmo quando o overview sozinho não traz exemplos de gasto), `answer(message, llm=None) -> AgentResponse` (RAG single-shot, não ReAct/tool-calling agentic: recupera docs, monta `CONTEXTO` + `PERGUNTA` em `HumanMessage` com `SYSTEM_PROMPT` em PT-BR, chama o LLM injetável — mesmo padrão `lru_cache`/`llm: BaseChatModel | None` do T20 — e devolve `AgentResponse(text=response.content)`) e `atendimento_node(state) -> dict` (lê `state["messages"][-1].content`, retorna `{"final_response": answer(...)}`). **Sem tool-calling real pelo LLM**: a recuperação é feita pelo código antes de chamar o modelo (grounding determinístico), não pelo LLM decidindo invocar a tool — mais simples e testável, evita depender do LLM real escolher a tool corretamente. `tests/unit/test_atendimento.py` mocka `vector.knowledge_seed.query_knowledge` (sem ChromaDB) e usa um `_EchoChatModel` que ecoa a última mensagem (simula um LLM respondendo apenas com base no CONTEXTO) — prova que os 5 nomes de categoria + faixa 30-40% + exemplo "aluguel" fluem do retrieval até `AgentResponse.text`. **Escopo**: `CONV-01` cobre só a AC1 ("plano de gastos"); citação de fonte (collection+doc, VEC-03) e o loop de tool-calling multi-turno não fazem parte de T21 — não implementados (spec-precision: T21 usa `query_knowledge` apenas para grounding interno, sem expor `doc_id` na resposta). **T22/T23** podem seguir o mesmo padrão (`answer`/`*_node` com `llm` injetável); **T24** (Validador) consome `AgentResponse.text`/`action` deste módulo; **T25** (wire graph) importa `atendimento_node` para substituir o branch de especialistas no grafo. Achado incidental (fora do escopo de T21, não mexido): `agents/specialists/transacoes.py` e `tests/unit/test_transacoes.py` já existiam como arquivos untracked no working tree antes desta task.
- **T22 note**: `agents/specialists/transacoes.py` expõe `parse_transaction_message(message) -> ParsedTransaction | None` (extração **por regex**, não LLM — marcadores "gastei/paguei/comprei"→despesa, "recebi/ganhei"→receita; valor via `R$ N` ou `N reais`; `None` quando tipo ou valor não são inferíveis, CHAT-03), `categorize(description, user_id, find_similar=None) -> BudgetCategory | None` (chama `chroma-mcp.find_similar_transactions` — já injetável para teste, mesmo padrão `llm=None`/`client=None` do T20/T18 — pega a categoria do primeiro hit com `metadata.category`; resultados já vêm ordenados por score do T17) e `transacoes_node(state, find_similar=None, create=None) -> dict` (dispatch por `state["intent"]`: `categorize` → só explica + `action="offer_register"`, **nunca chama** `create` — CONV-02; qualquer outro intent → `register_transaction`, que persiste via `finance-mcp.create_transaction` injetável). **Regra de categorização**: despesas exigem categoria inferida (se `categorize` retorna `None`, pede clarificação em vez de persistir sem categoria — CHAT-03); receitas pulam `categorize` inteiramente e persistem com `category=None` sempre (CHAT-02). **Spec-precision gap**: design não define como extrair tipo/valor/descrição da mensagem em linguagem natural — implementado via regex determinístico (não LLM) para manter os testes unitários rápidos/sem mock de chat model; descrição limpa é best-effort (remove o trecho do valor + verbo inicial), não testada por conteúdo exato. `tests/unit/test_transacoes.py` cobre os 3 cenários reais do spec (delivery→Prazeres sem persistir, cinema→despesa Prazeres persistida, salário→receita categoria NULL) + as duas clarificações (valor/categoria não inferíveis) — todos com fakes injetados, sem DB/ChromaDB/LLM real. **T23** (Orçamento) pode seguir o mesmo padrão de injeção (`get_budget_summary=None`); **T25** (wire graph) importa `transacoes_node` para o branch de especialistas.
- **T23 note**: `agents/specialists/orcamento.py` **reusa** a tool `get_budget_summary` do `finance-mcp` (T16) por import direto (`from mcp_servers.finance.server import get_budget_summary as _get_budget_summary`), sem reimplementar a lógica de orçamento — mesmo espírito de reuso do T22 (`create_transaction`/`find_similar_transactions`). Expõe `budget_advice(user_id, month=None, *, get_summary=None) -> AgentResponse` (injetável, mesmo padrão `find_similar=None`/`create=None` do T22) e `orcamento_node(state, *, get_summary=None) -> dict`. **CONV-04** tem prioridade: `has_income=False` retorna `NO_INCOME_ADVICE` sem sequer chamar a análise de categorias (`_prioritized_categories` nunca roda nesse caminho — testado via monkeypatch que falha se for chamada). **CONV-03**: categorias com `status == "alerta"` (acima da faixa) **ou** `remaining_pct <= TIGHT_MARGIN_PCT` entram na lista, ordenadas por `remaining_pct` crescente (mais crítica primeiro). **Spec-precision gap**: a spec não define um limiar numérico para "menor margem restante" quando a categoria ainda não estourou — escolhido `TIGHT_MARGIN_PCT = 5.0` pontos percentuais do teto. **Escopo (mesmo padrão do T21)**: T23 cobre apenas a pergunta "em quais categorias devo prestar atenção" (CONV-03); a pergunta "como está meu orçamento?" (BUD-03 AC3, resumo incondicional das 5 categorias) não tem um caminho dedicado — os dados das 5 categorias já estão disponíveis via `get_budget_summary` (T16/T10), mas a formatação de texto do especialista só lista as categorias que precisam de atenção; se `_prioritized_categories` não encontra nenhuma, devolve `NO_ATTENTION_TEXT` em vez de um resumo completo. Não estava nas Actions literais do T23 — sinalizado aqui, não implementado, para uma futura task decidir se cobre esse cenário. Achado incidental (fora do escopo desta task, não mexido): T21/T22 já estavam commitados (`62b951a`, `2644173`) quando T23 começou — trabalho paralelo de outra sessão, consistente com a nota "T21/T22/T23 paralelizáveis" do handoff anterior. **T24** (Validador) é o próximo consumidor de `AgentResponse`; **T25** (wire graph) importa `orcamento_node`.
- **T24 note**: `agents/validator.py` expõe `validate(response, *, user_id, month=None, get_balance=None, get_budget_summary=None) -> ValidationResult` (checks de design.md "Validator", nessa ordem: 1. reconstrói/confirma o contrato via `AgentResponse` ou `model_validate` de um dict — VAL-01; 2. texto não vazio; 3. heurística de PT-BR — presença de pelo menos uma stopword comum, ex. "que"/"não"/"você" — sem detecção de idioma real; 4. `suggested_category`, se presente, precisa ser um `BudgetCategory` válido; 5. todo valor `R$ N` e `N%` citado no texto precisa bater, com tolerância, contra um conjunto de valores conhecidos derivado de `finance-mcp.get_balance`/`get_budget_summary` para o mês — VAL-03/CONV-05) e `validator_node(state, *, get_balance=None, get_budget_summary=None) -> dict` (incrementa `state["validation_attempts"]`; aprovado → só avança o contador; rejeitado com tentativas < `MAX_VALIDATION_ATTEMPTS` (2) → `final_response=None` + nota em `agent_notes` — **este é o sinal que T25 deve usar na aresta condicional de retry ao Orquestrador**; tentativas esgotadas → substitui por `FALLBACK_TEXT` em vez de deixar o usuário sem resposta). **Spec-precision gap (escopo do check de R$)**: o valor de uma transação recém-registrada pelo especialista Transações (`AgentResponse.metadata["transaction"]["amount"]`, T22) é adicionado ao conjunto de valores conhecidos antes de checar — não é um agregado de `get_balance`/`get_budget_summary`, mas é exatamente o que acabou de ser persistido, então citá-lo de volta não é uma inconsistência (sem esse ajuste, toda confirmação de registro do T22 seria rejeitada). **T25** deve importar `validator_node` (substituindo o stub em `graph.py`) e adicionar a aresta condicional `validator -> orchestrator` quando `final_response is None`, e `validator -> END` caso contrário — mantendo `validation_attempts` como guarda contra loop infinito (já é incrementado por este node).
- **T25 note**: `agents/graph.py` liga todos os nós (`orchestrator`, `atendimento`, `transacoes`, `orcamento`, `validator`) no `StateGraph` — `_route_to_specialist(state) -> str` usa `orchestrator.specialist_for_intent(Intent(state["intent"]))` (só ORCH-01; **não** passa a `confidence` da classificação, então a aresta de roteamento não aplica o override de ambiguidade do ORCH-02 — `orchestrator_node` continua retornando só `{"intent": ...}`, contrato já fixado pelo teste `test_orchestrator_node_sets_intent_on_state` do T20, que eu não alterei; fechar ORCH-02 no nível do grafo fica para uma task futura, ORCH-02 continua **Partial**). `_route_after_validation(state) -> str` volta a `"orchestrator"` quando `state["final_response"] is None` (sinal que `validator_node`/T24 já emite numa rejeição não esgotada) ou `END` caso contrário — como `validator_node` incrementa `validation_attempts` e preenche `final_response` com o fallback ao esgotar `MAX_VALIDATION_ATTEMPTS` (2), o loop sempre termina (confirmado manualmente: 2 chamadas ao orquestrador antes do fallback, sem estourar o `recursion_limit` default do LangGraph). `run(user_id, session_id, message, *, graph=None) -> AgentResponse` é a interface literal de design.md: monta o `AgentState` inicial, invoca o grafo compilado (compila um novo com `build_graph()` se nenhum for injetado — testes injetam para reusar o mesmo grafo) e persiste o turno via `_persist_turn` (dois `ChatMessage`, role `"user"`/`"assistant"`, mesmo `session_id`) — usa `SessionLocal` importado de `db.session` como atributo de módulo (mesmo padrão de monkeypatch do T16/T17: testes fazem `monkeypatch.setattr(graph_module, "SessionLocal", testing_session)`). **Não cria** `ChatSession` — só `chat_messages` foi pedido pela Action do T25; `ChatMessage.session_id` não tem FK para `chat_sessions.session_id` no schema (T3), então não há pendência de integridade. `tests/integration/test_graph_smoke.py` cobre o caminho aprovado (orcamento, 1 chamada ao orquestrador) e o caminho de rejeição total (validator sempre vê um `get_budget_summary` inconsistente com o que o especialista usou → 2 tentativas → fallback), além de checar as duas linhas persistidas em `chat_messages` por sessão. **T27+** (dashboard) e **T29** (chat/SSE) são os primeiros consumidores reais de `agents.graph.run()`.
- **T27 note**: `web/router.py`'s `/dashboard` substitui o stub do T7 — monta `BudgetService(db).get_summary(user.id, month)` (T10) + `TransactionRepository(db).list(user.id, month=month)` (T9) para o mês corrente (`date.today().strftime("%Y-%m")`) e renderiza `web/templates/dashboard.html` (`{% extends "base.html" %}`, primeiro consumidor real do layout do T26). `CATEGORY_LABELS` (novo dict em `web/router.py`, `BudgetCategory` → nome em PT-BR, ex. `custos_fixos` → "Custos Fixos") só existe para exibição — nenhum outro módulo tinha esse mapeamento centralizado; specialists/knowledge_seed continuam com os nomes inline nos próprios textos. **Sentinela do teto de Prazeres (T4)**: o template mostra `(faixa X–Y%)` para categorias normais, mas quando `max_pct >= 100` (hoje só Prazeres) mostra `(faixa ≥X%)` — trata o teto artificial de `100.0` (T4's spec-precision gap) como "sem teto real" em vez de exibir "≥5–100%" de forma enganosa. Empty state (WEB-03) usa `summary.warning` (`NO_INCOME_WARNING`, T10) para o aviso de receita ausente e checa `transactions` vazio para o aviso de mês sem lançamentos — ambos apontam para `/chat` (ainda não existe, T29 cria a rota). CSS novo: `.summary-line`/`.empty-state` em `style.css`. `tests/integration/test_dashboard.py` cobre WEB-01 (tabela com as 5 colunas), WEB-02 (5 cards, incluindo o caso `alerta` quando uma categoria estoura a faixa) e WEB-03 (estado vazio), reusando o padrão de `client`/`get_db` override do `test_auth.py`. **T28** (filtros HTMX) deve reusar `TransactionRepository.list`'s parâmetros `month`/`category`/`type` já existentes (T9) — nenhuma mudança no repositório é esperada.
- **T28 note**: `/dashboard/transactions` (novo em `web/router.py`) aceita `month`/`category` opcionais via query string e devolve só o partial `_transactions_table.html` (sem `<html>`/base layout) — extraído do próprio `dashboard.html` do T27 para ser reusado pelos dois (a rota completa `/dashboard` agora também faz `{% include "_transactions_table.html" %}` em vez de duplicar a tabela). `month=""` explícito limpa o filtro (all-time), não retorna zero linhas — `month_filter = month or None` trata string vazia e ausência de query param da mesma forma. `category` inválida (fora do enum `BudgetCategory`) → `HTTPException(400)` via `_parse_category` (nenhuma spec definia esse caso; escolhido por consistência com os outros 400 de validação do projeto). `dashboard.html` ganhou um `<form class="filters" hx-get="/dashboard/transactions" hx-target="#transactions-table" hx-trigger="change">` com `<input type="month">` + `<select>` de categoria — HTMX troca só a `div#transactions-table` (WEB-04, "sem reload completo"); o form fica fora do alvo trocado, então o navegador preserva os valores selecionados entre trocas sem esforço extra. **Ajuste retroativo**: a mensagem de estado vazio ficou "Nenhuma transação registrada..." (idêntica ao T27, só ganhou o trecho opcional de categoria) para não quebrar o teste `test_dashboard_shows_empty_state_without_transactions` do T27 — cheguei a escrever "encontrada" primeiro e revertido. Também marquei retroativamente TBL-01/TBL-02 como **Done** (T27) na tabela de rastreabilidade — a tabela+empty-state do T27 já cobria essas duas, só não tinham sido atualizadas na ocasião.
- **T29 note**: novo pacote `chat/router.py` (já existia `chat/__init__.py` vazio desde a estrutura do T1) expõe `GET /chat` (`get_current_user`, redireciona pro login — mesmo padrão do `/dashboard`) e `POST /api/chat` (`get_current_user_api`, 401 se não autenticado — AUTH-06) com body `ChatRequest(message, session_id)` (Pydantic, literal de design.md). `POST /api/chat` chama `agents.graph.run(user_id, session_id, message)` (T25 — já persiste o turno em `chat_messages`) e devolve `StreamingResponse(media_type="text/event-stream")` com **um único evento SSE** carregando `AgentResponse.model_dump_json()`, seguido de um evento `event: done`. **Spec-precision gap**: design.md pede `astream_events` para tokens incrementais, mas nenhum especialista (T21-23) usa streaming no LLM — todos chamam `.invoke()` bloqueante — então não há nada incremental para repassar; implementar isso exigiria reescrever os 3 especialistas para streaming, fora do escopo do T29. O protocolo SSE (framing `data:`/`event:`) já está correto; upgrade para token-a-token fica para uma task futura quando os especialistas usarem streaming. `web/templates/chat.html` é uma UI mínima (form + `fetch` lendo `response.body.getReader()` manualmente, sem `EventSource` porque é um POST — `EventSource` só suporta GET) — não testável via pytest (é JS de browser), só a renderização do template e os endpoints HTTP são cobertos por `tests/integration/test_chat.py`. `main.py` registra `chat_router` depois do `web_router`. Marquei CHAT-01/02/03 e WEB-05 como **Done** na tabela de rastreabilidade — o `Requirement → Task Map` do `tasks.md` já apontava T29 como a última task de CHAT-01/02/03 (T30-32 são de CONV-01..05, uma tabela diferente).
- **Verify note (T10+)**: `rtk pytest`/`pytest` global NÃO coletam (pacote não instalado no env global) — rodar testes com `.venv/bin/python -m pytest tests/unit/test_budget_service.py -m unit` (suíte unit inteira: 68 testes verdes; 107 após T21; 115 após T22; 123 após T23; 139 após T24; suíte completa unit+integration: 172 após T25, 176 após T27, 183 após T28, 187 após T29 — `.venv/bin/python -m pytest tests/ -m "unit or integration" --tb=short`).
- **Branch**: `master` (git repo inicializado em T1)
- **Env note**: sistema sem `python3-venv`/`ensurepip`; venv em `.venv/` com pip via `get-pip.py`. Usar `.venv/bin/python` para todos os comandos.
